// ZUSTAND GLOBAL STATE STORE //

// import { create } from "zustand";

// const useBroadcastStore  = create((set) => ({
//     message: [],
//     raiseNewMessage: (message) => set(() => ({ message.push }))
// }));